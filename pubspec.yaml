name: smartshop
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.27.1
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.15.10
  firebase_storage: ^11.7.7
  image_picker: ^1.0.7



  # Icons
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.1.6
  firebase_database: ^10.4.0
  firebase_remote_config: ^4.4.7

  # AI Chatbot - Google Gemini
  google_generative_ai: ^0.2.1

  # Utilities
  http: ^1.2.0
  provider: ^6.1.1

  # UI Enhancements
  flutter_chat_bubble: ^2.0.0
  lottie: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/banners/  # For Lottie animations
